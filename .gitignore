# === 原始素材：体积大 + 版权，绝不入库 ===
*.pdf
/tmp/
pipeline/**/raw/
*.raw.txt
saa-c03.txt

# === 凭证 ===
.env
.env.local
*.local
*.pem
*.key

# === Go ===
/backend/bin/
/backend/tmp/
*.test

# === Node / Next.js ===
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# === Python ===
__pycache__/
*.pyc
.venv/
venv/

# === 编辑器 / 系统 ===
.idea/
.vscode/
.DS_Store
Thumbs.db

# 注：data/**/*.json（题库中间产物）**故意纳入版本管理** ——
# 它是 source of truth，需要 review 和 diff。前提是本仓库永久 private。
#
# 唯一例外：enriched.json 是 enriched/*.json 分片的 merge 产物，
# 每完成一片就要全量重写 1.5MB，进 git 只是无谓膨胀。
# 需要时一条命令重建：python3 pipeline/core/enrich.py merge <bank>
data/*/enriched.json
